import React, { useState, useEffect } from "react";
import { Row, Col, Button, Card, CardBody, CardTitle, Modal, Table } from "reactstrap";

const FeedbackList = () => {
    return (
        <Card>
            <CardBody>
                <CardTitle className="mb-4 text-center">
                    <h2>Feedback list</h2>
                </CardTitle>
            </CardBody>
        </Card>
    );
};

export default FeedbackList;
